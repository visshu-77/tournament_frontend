PORT=8080
# Base path for the app (used by Vite config)
BASE_PATH=/
# API base URL the frontend will call. Use http://localhost:5000 for local backend.
# Vite exposes env vars prefixed with VITE_ to the browser.
VITE_API_BASE=http://localhost:5000

